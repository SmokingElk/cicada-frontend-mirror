import { Styleable } from "@/lib/types";
import { cn } from "@/lib/utils";

export default function GameOpponentsInfo({ className = "" }: Styleable) {
  return <div className={className}>{/* Вася, это для тебя */}</div>;
}
